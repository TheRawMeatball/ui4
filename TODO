tracked range
use change detection in component observer s for change hint
query::get type observer
portals
lenses

transition support in lists
trait hackery to enable builder pattern widgets

[HARD] switch to morphorm + epaint and ditch bevy_ui

docs